A web form: enter a URL and a query, MolmoWeb runs, you see what it did. We need this so we can iterate quickly with MolmoWeb.

Literally just input (Url, query) -> run it -> MW runs -> returns traj.save_html() and the agent output. the html will also show rendered screenshots with click dots, reasoning text, and action json (MW has this built in yay!!!)

I will try to host this so that we can all access it and iterate on it. I will host this website from the cluster. We steal a GPU for a while... I will try to have a second laptop which is always on and has a terminal which is interacting with the GPU session so that its always on and we can always just run it

if needed you guys might have to download tailscale but i'll keep posted